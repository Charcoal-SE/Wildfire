<%= form_for(@flag) do |f| %>
  <% if @flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flag.errors.count, "error") %> prohibited this flag from being saved:</h2>

      <ul>
      <% @flag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :completed %><br>
    <%= f.check_box :completed %>
  </div>
  <div class="field">
    <%= f.label :failure_reason %><br>
    <%= f.text_field :failure_reason %>
  </div>
  <div class="field">
    <%= f.label :attempted_at %><br>
    <%= f.datetime_select :attempted_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
